const movies = ["fast 9", "Marvel", "Dc", "Avatar", "fast 5"];

const movieList = movies.filter((movie) => movie.includes("fast"));
//return a array of the matching condition

console.log(movieList);
